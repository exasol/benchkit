name: "Performance Benchmarks"
version: "2.0.0"
description: "Community-contributed database performance benchmarks"
author: "Benchkit Community"
results_dir: "results"

series:
  tpch:
    name: "TPC-H"
    description: "TPC-H benchmark across all scale factors, streams, and node counts"
  clickbench:
    name: "ClickBench"
    description: "ClickBench web analytics benchmark (100M rows)"
    enabled: false
  estuary:
    name: "Estuary"
    description: "Estuary warehouse benchmark"
    enabled: false

publish:
  title: "Performance Benchmark Leaderboard"
  template: "leaderboard"
  include_reports: true

# Series 2: Scale Factor Scaling - SF50
# Purpose: Measure data scalability on single node with mid-range dataset
# Variable: SF (50)
# Fixed: Nodes=1, Streams=4
# Systems: All 5 (Exasol, ClickHouse, Trino, StarRocks, DuckDB)
project_id: "extscal_sf_50"
title: "Streamlined Scalability - Scale Factor 50 (Single Node)"
author: "Benchmark Team"

execution:
  parallel: true
  max_workers: 5

env:
  mode: "aws"
  region: "eu-west-1"
  allow_external_database_access: true
  instances:
    exasol:
      instance_type: "r6id.2xlarge"  # 8 vCPUs, 64GB RAM
      disk:
        type: "local"
      label: "extscal-exa-sf50"
    clickhouse:
      instance_type: "r6id.2xlarge"  # 8 vCPUs, 64GB RAM
      disk:
        type: "local"
      label: "extscal-ch-sf50"
    trino:
      instance_type: "r6id.2xlarge"  # 8 vCPUs, 64GB RAM
      disk:
        type: "local"
      label: "extscal-trino-sf50"
    starrocks:
      instance_type: "r6id.2xlarge"  # 8 vCPUs, 64GB RAM
      disk:
        type: "local"
      label: "extscal-sr-sf50"
    duckdb:
      instance_type: "r6id.2xlarge"  # 8 vCPUs, 64GB RAM
      disk:
        type: "local"
      label: "extscal-duck-sf50"
  os_image: "ubuntu-22.04"
  ssh_key_name: "ok-work3"
  ssh_private_key_path: "~/.ssh/ok-work3.pem"

systems:
  # Exasol - single node, 64GB instance
  - name: "exasol"
    kind: "exasol"
    version: "2025.2.0"
    setup:
      method: "installer"
      node_count: 1
      use_additional_disk: true
      c4_version: "4.28.5"
      host_addrs: "$EXASOL_PRIVATE_IP"
      host_external_addrs: "$EXASOL_PUBLIC_IP"
      image_password: "exasol123"
      db_password: "exasol456"
      admin_password: "exasol789"
      license_file: "configs/exasol.license"
      db_mem_size: 48000  # ~75% of 64GB
      extra:
        optimizer_mode: "analytical"
        db_params:
          - "-writeTouchInit=1"
          - "-cacheMonitorLimit=0"
          - "-maxOverallSlbUsageRatio=0.95"
          - "-useQueryCache=0"
          - "-query_log_timeout=0"
          - "-joinOrderMethod=0"
          - "-etlCheckCertsDefault=0"

  # ClickHouse - single node, 64GB instance
  - name: "clickhouse"
    kind: "clickhouse"
    version: "25.10.2.65"
    setup:
      method: "native"
      node_count: 1
      use_additional_disk: true
      data_dir: "/data/clickhouse"
      host: "$CLICKHOUSE_PUBLIC_IP"
      port: 8123
      username: "default"
      password: "clickhouse123"
      extra:
        memory_limit: "48g"
        max_threads: "8"
        # 64GB instance, 4 streams: allow 15GB/query, grace_hash spills to disk
        max_memory_usage: "15000000000"
        max_bytes_before_external_group_by: "5000000000"
        max_bytes_before_external_sort: "5000000000"
        join_algorithm: "grace_hash"
        max_bytes_in_join: "10000000000"

  # Trino - single node (no S3 needed for single node)
  - name: "trino"
    kind: "trino"
    version: "479"
    setup:
      method: "native"
      node_count: 1
      use_additional_disk: true
      host: "$TRINO_PUBLIC_IP"
      port: 8080
      username: "admin"
      catalog: "hive"
      schema: "tpch_views"
      extra:
        query_max_memory: "48GB"
        # 64GB RAM: heap=51GB, safe query memory = 51*0.7 = 35GB
        query_max_memory_per_node: "35GB"

  # StarRocks - single node
  - name: "starrocks"
    kind: "starrocks"
    version: "4.0.4"
    setup:
      method: "native"
      node_count: 1
      use_additional_disk: true
      host: "$STARROCKS_PUBLIC_IP"
      port: 9030
      username: "root"
      password: ""
      extra:
        bucket_count: 4
        replication_num: 1

  # DuckDB - embedded single node
  - name: "duckdb"
    kind: "duckdb"
    version: "1.4.4"
    setup:
      method: "native"
      use_additional_disk: true
      data_dir: "/data/duckdb"
      database_path: "/data/duckdb/benchmark.duckdb"
      extra:
        memory_limit: "48GB"  # ~75% of 64GB
        threads: 8

workload:
  name: "tpch"
  scale_factor: 50
  runs_per_query: 5
  warmup_runs: 1
  data_format: "csv"
  generator: "dbgen"
  system_variants:
    clickhouse: "tuned"
  multiuser:
    enabled: true
    num_streams: 4
    randomize: true
    random_seed: 42

report:
  show_boxplots: true
  show_bar_chart: true
  show_heatmap: true
